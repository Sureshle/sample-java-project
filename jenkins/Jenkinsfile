pipeline 
{
    agent {
    node {
        label 'Maven'
        
    }
    }
    stages {

        stage('Build') {

            steps {
withMaven(jdk: 'JAVA', maven: 'maven') 
             {
bat'mvn clean compile'
}
            }
        }
                   
            }
            
}
